{% extends 'layout.html.j2' %}

{% block content %}
    <h1>Zoteroxy</h1>

    <p>This is instance of Zotero proxy service to publish certain items from otherwise private library of Zotero. It provides both simple web interface but also JSON API.</p>

    <h2>Configuration</h2>

    <ul>
        <li>Library: <strong>{{ config.library.name }}</strong> (ID: {{ config.library.id }})</li>
        <li>Filter based on tags:
        <ul>
            {% for tag in config.settings.tags %}
                <li><span class="badge badge-success">{{ tag }}</span></li>
            {% else %}
                <li><em>No tags are configured...</em></li>
            {% endfor %}
        </ul>
        </li>
    </ul>

    <h2>Capabilities</h2>

    <ul>
        <li>Custom JSON collection of filtered Zotero library items</li>
        <li><a href="http://www.bibtex.org" target="_blank">BibTeX</a> serialization of the collection items</li>
        <li><a href="http://okfnlabs.org/bibjson/" target="_blank">BibJSON</a> serialization of the collection items</li>
        <li>File attachments retrieval</li>
    </ul>

    <h2>Usage</h2>

    <p>For more details visit the <a href="/swagger-ui/" target="_blank">Swagger API documentation</a>.</p>

    <ul>
        <li><code>GET /</code> = basic information about the proxy</li>
        <li><code>GET /collection</code> = list of published items</li>
        <li><code>GET /settings</code> = settings and service info of the proxy</li>
    </ul>
{% endblock %}
